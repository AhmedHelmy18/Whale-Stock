import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Bootstrap test', (WidgetTester tester) async {
    // Basic test to avoid empty test file warnings
    expect(true, isTrue);
  });
}
